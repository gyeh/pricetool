version: "2"
sql:
  - engine: "postgresql"
    queries: "parser/sql/queries.sql"
    schema: "parser/sql/migrations"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
        emit_exact_table_names: false
